// src/utils/helpers.js

/**
 * A placeholder for general utility functions that might be needed later.
 * For example, validation functions, data formatting, etc.
 */

// Example: A simple utility function (not currently used in the main logic)
export const capitalizeFirstLetter = (string) => {
    if (!string) return '';
    return string.charAt(0).toUpperCase() + string.slice(1);
  };
  
  // Add more helper functions as your application grows.
  